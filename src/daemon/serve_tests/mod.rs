//! What the daemon serves to attached clients.
//!
//! Split by what each group is about — the shared repository set, and the
//! terminals the session runs — over one set of fixtures in `harness`.

mod accent;
mod active;
mod harness;
mod session;
mod terminals;
